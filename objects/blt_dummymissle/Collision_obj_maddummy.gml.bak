if(normal == 1 && destroy == 0) {
    speed= 0;
    // obj_maddummy
    with(287) event_user(3);
    if(obj_maddum_drawer.alarm[5] < 2) {
        // obj_maddum_drawer
        with(288) event_user(2);
    }
    // obj_maddummy
    with(287) event_user(7);
    if(!instance_exists(obj_shaker )) scr_shake(4, 4, 2);
    destroy= 1;
    normal= 0;
}


/*  */
