function scr_textskip() {
	if(instance_exists(OBJ_WRITER ) && keyboard_multicheck_pressed(1)) {
	    // OBJ_WRITER
	    with(782) stringpos= string_length(originalstring);
	    keyboard_clear(16);
	}



}
